# SENPRO — Sitio personal

Scaffold inicial con Next.js (App Router), TailwindCSS, Framer Motion y GSAP.

Instrucciones rápidas:

1. Instalar dependencias:

```
npm install
```

2. Ejecutar en desarrollo:

```
npm run dev
```

Reemplaza `/public/hero.mp4` y añade tu `logo.svg`.
